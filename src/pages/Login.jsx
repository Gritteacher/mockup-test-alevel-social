import { useEffect,useState } from 'react'; import { useNavigate,Link } from 'react-router-dom'; import { GraduationCap,ShieldCheck,ArrowLeft } from 'lucide-react'; import { supabase,isSupabaseConfigured } from '../lib/supabaseClient'
export default function Login(){const nav=useNavigate();const [loading,setLoading]=useState(false);useEffect(()=>{if(isSupabaseConfigured)supabase.auth.getSession().then(({data})=>data.session&&nav('/student/dashboard'))},[]);async function login(){setLoading(true);if(!isSupabaseConfigured){setTimeout(()=>nav('/student/dashboard'),450);return}const redirectTo=new URL(`${import.meta.env.BASE_URL}student/dashboard`,window.location.origin).toString();const {error}=await supabase.auth.signInWithOAuth({provider:'google',options:{redirectTo}});if(error){alert('ไม่สามารถเข้าสู่ระบบได้ กรุณาลองใหม่');setLoading(false)}}return <div className="auth-page"><Link to="/" className="back-home"><ArrowLeft/>กลับหน้าหลัก</Link><div className="login-card"><span className="big-brand"><GraduationCap/></span><div className="eyebrow">ยินดีต้อนรับ</div><h1>เข้าสู่ระบบเพื่อเริ่มฝึกฝน</h1><p>ใช้บัญชี Gmail ของคุณเพื่อบันทึกคะแนน<br/>และติดตามพัฒนาการอย่างต่อเนื่อง</p><button className="google-button" onClick={login} disabled={loading}><span className="google-g">G</span>{loading?'กำลังเข้าสู่ระบบ…':'ดำเนินการต่อด้วย Gmail'}</button><div className="security-note"><ShieldCheck/>ข้อมูลของคุณจะถูกเก็บอย่างปลอดภัย</div><small>เมื่อเข้าสู่ระบบ ถือว่าคุณยอมรับเงื่อนไขการใช้งาน<br/>และนโยบายความเป็นส่วนตัว</small></div></div>}
