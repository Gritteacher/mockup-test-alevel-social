import { useEffect,useState } from 'react'; import { Search,Plus,Minus,Users } from 'lucide-react'; import { supabase,isSupabaseConfigured } from '../../lib/supabaseClient'
const demo=[{id:'u1',full_name:'ณัฐชา ตั้งใจเรียน',email:'natcha@gmail.com',school:'โรงเรียนสาธิตศึกษา',grade:'ม.6',mock_quota:3,practice_points:240},{id:'u2',full_name:'ธนกฤต ใฝ่รู้',email:'thanakrit@gmail.com',school:'เตรียมอุดมศึกษา',grade:'ม.6',mock_quota:1,practice_points:85},{id:'u3',full_name:'ปภาวี พัฒนา',email:'papavee@gmail.com',school:'สามเสนวิทยาลัย',grade:'ม.5',mock_quota:0,practice_points:60}]
export default function Students(){const [rows,setRows]=useState(demo),[q,setQ]=useState('');useEffect(()=>{if(isSupabaseConfigured)supabase.from('profiles').select('*, quota_wallets(*)').eq('role','student').then(({data})=>data&&setRows(data.map(x=>({...x,...x.quota_wallets?.[0]}))))},[]);async function adjust(id,amount){if(isSupabaseConfigured){const {error}=await supabase.rpc('admin_adjust_quota',{p_user_id:id,p_amount:amount,p_note:amount>0?'เพิ่มโควตาโดยครู':'ลดโควตาโดยครู'});if(error)return alert('ปรับโควตาไม่สำเร็จ: '+error.message)}setRows(rows.map(x=>x.id===id?{...x,mock_quota:Math.max(0,x.mock_quota+amount)}:x))}const filtered=rows.filter(x=>(x.full_name+x.email).toLowerCase().includes(q.toLowerCase()));return <div className="page"><div className="page-heading"><div><span className="eyebrow">จัดการผู้เรียน</span><h1>นักเรียน</h1><p>ดูข้อมูลและจัดการโควตาของนักเรียนทั้งหมด</p></div><span className="count-badge"><Users/>{rows.length} คน</span></div><div className="panel table-panel"><div className="table-toolbar"><label className="search"><Search/><input placeholder="ค้นหาชื่อหรืออีเมล…" value={q} onChange={e=>setQ(e.target.value)}/></label></div><div className="table-scroll"><table><thead><tr><th>นักเรียน</th><th>โรงเรียน</th><th>ระดับชั้น</th><th>โควตา</th><th>Practice Points</th><th>จัดการ</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td><div className="student-cell"><span className="avatar">{x.full_name[0]}</span><div><b>{x.full_name}</b><small>{x.email}</small></div></div></td><td>{x.school}</td><td>{x.grade}</td><td><b className={x.mock_quota<1?'bad':''}>{x.mock_quota}</b></td><td>{x.practice_points}</td><td><div className="quota-buttons"><button onClick={()=>adjust(x.id,-1)} title="ลดโควตา"><Minus/></button><button onClick={()=>adjust(x.id,1)} title="เพิ่มโควตา"><Plus/></button></div></td></tr>)}</tbody></table></div></div></div>}
